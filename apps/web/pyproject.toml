[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "echo-mvp-web"
version = "0.1.0"
description = "Echo MVP Streamlit frontend"
requires-python = ">=3.11"
dependencies = [
  "streamlit>=1.38.0",
  "requests>=2.32.0"
]
